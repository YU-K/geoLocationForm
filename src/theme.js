import { deepPurple, amber } from "@material-ui/core/colors";

const theme = {
  palette: {
    primary: {
      main: deepPurple[500],
    },
    secondary: {
      main: amber[500],
      contrastText: deepPurple[900],
    },
  },
};

export default theme;
